<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.ApplicationMapper">


    <select id="selectAll" resultType="com.example.entity.Application">
        select application.*,`club`.name as clubName,`student`.name as userName from `application`
        left join `club` on application.club_id = `club`.id
        left join `user` on application.student_id = `user`.id
        <where>
            <if test="status != null">and status = #{status}</if>
        </where>
        order by create_time desc
    </select>

    <insert id="insert">
        insert into `application`(student_id,club_id,status,reason,approver_id,create_time,approve_time)
        values(#{studentId},#{clubId},#{status},#{reason},#{approverId},#{createTime},#{approveTime})
    </insert>

    <update id="updateStatus">
        update application
        set status = #{status}, approver_id = #{approverId}, approve_time = #{approveTime}, remark = #{remark}
        where id = #{id}
    </update>

    <update id="update">
        update `application` set student_id=#{studentId},club_id=#{clubId},status=#{status},reason=#{reason},approver_id=#{approverId},approve_time=#{approveTime},remark=#{remark} where
        id=#{id}
    </update>

</mapper>